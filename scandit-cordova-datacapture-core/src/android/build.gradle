android {
    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
  }
}

repositories {
    flatDir {
        dirs 'libs', 'src/main/libs'
    }
}

dependencies {
    api(name: 'ScanditCaptureCore', ext: 'aar')
}
